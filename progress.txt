Progress log for Hue and You. Newest entry at the top. Keep only the 5 newest entries.

## 2026-09-25 - Issue #23: Simplified Match Object result - Complementary, Analogous, Tonal with info dialogs and neutral hint

`HarmonyRelationship`/`HarmonyEngine.generate` dropped `SPLIT_COMPLEMENTARY`/`TRIADIC` entirely
(enum cases, generation, tests) - only Complementary, Analogous and Tonal remain. Neutral-color
detection is a separate pure function, `HarmonyEngine.isNeutral(chroma)` against a new
`HarmonyConfig.NEUTRAL_CHROMA_THRESHOLD = 12.0`, rather than folded into `generate()` - keeping it
outside `generate()` meant the existing hue/balance tests (which run over arbitrary sample colors)
didn't need auditing for accidental neutrality, and the threshold itself stays boundary-testable
as a plain `Double -> Boolean` (same shape as `ColorMatchBand.forDeltaE`) instead of needing a
gamut-mapped round trip through an actual color.

`HarmonyResultBody` (shared by the live Match Object result and History detail) is now just the
relationship rows: no input-color swatch/hex/label section, no disclaimer text. Each row is a
compact `Row` - label + a small ⓘ `IconButton` (32dp touch target, so it doesn't blow out row
height) on the left, ~64dp circles on the right - and tapping the ⓘ opens a new shared
`InfoDialog` (title + one/two sentences + a single OK button, `ui/common/InfoDialog.kt`) with the
fixed explanation text for that relationship. When `HarmonyEngine.isNeutral(chroma)` is true,
Complementary/Analogous are swapped for one plain hint row; Tonal always renders. The same
`InfoDialog` backs a new ⓘ action in `MatchObjectScreen`'s and `HistoryDetailScreen`'s top bars
(History only when the entry is `OBJECT`) showing the best-guess disclaimer that used to sit
inline - reusing `rate_clothing_best_guess_disclaimer` rather than a new string, since Rate
Clothing's own inline disclaimer (`PaletteResultBody`) is untouched and out of scope.

Wheel/balance and the rename field are gone from both the live result page and History detail -
`HarmonyOptionsControls` now has exactly one caller left (Settings' own defaults, unchanged); new
results still take Settings' defaults, History still reopens with the wheel/balance it was saved
with, but neither is editable from the result/detail screens anymore. Since nothing could still
call it, `HistoryRepository.updateHarmonyOptions`/`HistoryDao.updateHarmonyOptions` (and the SQL
`UPDATE ... SET wheel, balance` query) were deleted outright rather than left as dead code, along
with `MatchObjectViewModel.renameResult`/`setWheel`/`setBalance` and
`HistoryDetailViewModel.setWheel`/`setBalance` and the now-unused `historyEntryName` state field -
the `wheel`/`balance` columns themselves are untouched (still written at creation, still read for
display), so no migration was needed.

Test: `HarmonyEngineTest` lost its split-complementary/triadic assertions and gained
`isNeutralExcludesTheThresholdItselfButIncludesJustBelowIt` (boundary test at exactly
`NEUTRAL_CHROMA_THRESHOLD`). No new Compose UI test for the row/dialog layout or the "fits without
scrolling" AC (same Robolectric/JDK-26 gap noted since #3) - verified by reading through the
layout and manually reasoning about removed vertical space (no hex/name/wheel-balance/disclaimer
lines). `./gradlew :colorspace:test :app:testDebugUnitTest assembleDebug` all pass.

## 2026-09-25 - Issue #22: Result page photo, alternative color chips and tap-to-pick

Both result pages (`MatchObjectScreen`, `RateClothingScreen`) now show the decoded photo at the
top (capped at 35%/40% of screen height via a new shared `PhotoResultSection` composable that
measures the actual `BoxWithConstraints` width so the image never letterboxes), a marker (the
center 40% box for the auto-pick/a chip-pick, or a ring at the tap point sized to the real sample
circle), and a `ColorChipRow` of the current color plus up to 4 alternatives. The decoded `Bitmap`
now lives in `ShowingResult` state itself (both ViewModels) instead of going out of scope after
extraction, since it's needed for display and for re-sampling on a tap.

`ColorExtractor` gained `extractCandidates` (every quantized cluster in a region, no min-share
filter - `extractMainColor` now just takes `.first()`), `extractColorAtPoint` (dominant color in a
circle of radius ~3% of the image's short edge, for tap-to-pick), and `selectAlternatives` (greedy
ΔE00 ≥ 5 dedup against the current pick and against each other, capped at 4 -
`CalibrationConfig.ALTERNATIVE_MAX_COUNT`/`ALTERNATIVE_MIN_DELTA_E`/`TAP_SAMPLE_RADIUS_FRACTION`).
Both ViewModels cache the ranked candidate list from the initial center-box extraction per photo;
a chip re-pick just re-filters that cached list (synchronous, no pixel work), while a tap re-pick
re-samples via `extractColorAtPoint` off `backgroundDispatcher` before filtering the same cached
list for new alternatives - both funnel through a shared private `applyPick` that updates state
immediately and persists via a new `updateObjectPick`/`updateClothingPick` repository call, which
`UPDATE`s the existing History row in place (color, sample point, and recomputed score for
Clothing) rather than inserting a new entry. (`pickAtPoint` reads width/height from the injected
`PixelSource`, not from the real `Bitmap`, so it stays unit-testable without Robolectric.)

`HistoryEntry`/`HistoryEntryEntity` gained nullable `sampleX`/`sampleY` (normalized 0-1; null =
center box). Room bumped to version 4 with a real `MIGRATION_3_4` (`ALTER TABLE ... ADD COLUMN ...
REAL`, additive/nullable) registered via `addMigrations` ahead of the existing
`fallbackToDestructiveMigration` safety net - the first real migration in this codebase (prior
version bumps just relied on destructive fallback). Since Room migration testing normally needs
Robolectric/instrumentation (the gap this codebase already avoids elsewhere, see
`DefaultBackupRepository`'s `TransactionRunner`), `Migration3To4Test` instead asserts on the exact
SQL via a recording `java.lang.reflect.Proxy` over `SupportSQLiteDatabase` - same "no real DB"
philosophy as the DAO fakes, one level lower. Backup gained matching optional `sampleX`/`sampleY`
(entry, DTO, export, import); no schema version bump needed since `ignoreUnknownKeys` plus
nullable-with-null-default fields make old backups without the fields import unchanged (tested).

Test: `ColorExtractorTest`/new `ColorExtractorSelectAlternativesTest` cover the three new
colorspace functions (candidate ranking with no share filter, circle sampling and its bounds
clamping, dedup/cap/order for alternatives). `MatchObjectViewModelTest` gained
`pickCandidate_updatesStateInstantlyAndPersistsToTheSameEntry` and
`pickAtPoint_samplesOffMainThreadThenUpdatesTheSameEntry` (same off-main-thread assertion shape as
#20/#21); Rate Clothing still has no dedicated ViewModel test (unchanged since #20) but its
`updateClothingPick` path is covered at the repository layer. `DefaultHistoryRepositoryTest` and
`BackupSerializerTest` gained cases for the new update/roundtrip/missing-field paths. History
detail screen re-pick (chips there) is explicitly out of scope - that's #27.
`./gradlew :colorspace:test :app:testDebugUnitTest assembleDebug` all pass.

## 2026-09-25 - Issue #21: Skip white calibration and auto-pick the main color from the center box

Deleted `WhiteBalanceCalibrator` and its config constants/test wholesale (dead code, no more tap-to-
calibrate step anywhere), plus the now-unused `CircleRegion` (it only existed to describe the white-
sheet sample circle `ColorExtractor` excluded). `ColorExtractor.extract()` (candidate-list-with-share-
filter, `isClearlyDominant`) is replaced by a single `extractMainColor(pixels, region = centerBox)` -
quantizes with the same Celebi quantizer and returns the single largest-share `argb`, no `Double?`
share or candidate list to plumb through. `RectRegion.centerBox(width, height, fraction)` computes the
middle-40%-by-40% box (`CalibrationConfig.CENTER_BOX_FRACTION`); `CalibrationConfig` lost every
calibration/dominance/share-filter constant that only that old API used.

Both `MatchObjectViewModel` and `RateClothingViewModel` lost their `Calibrating`/`SelectingColor(s)`
states, `onTap`/`confirmCalibration`/`chooseNewPhoto`/(for Match Object)
`toggleColorSelection`/`confirmColorSelection` entirely. `onPhotoPicked` now decodes the bitmap, flips
to `ExtractingColors`, then a private `extractAndSaveResult(bitmap)` calls `extractMainColor` on
`backgroundDispatcher`, saves the history entry, and goes straight to `ShowingResult` - photo to result
in one step, no `currentBitmap` field needed since the bitmap is a local all the way through. Match
Object's `saveObjectResult` now always gets a single-element `inputColorsArgb` list; the stored field
name and repository signature are unchanged (still a `List<Int>`, per the AC - no migration).
`HarmonyResultBody` (shared by the live Match Object result and History detail) now takes one
`inputColorArgb: Int` instead of iterating a list, so `HistoryDetailScreen` passing
`entry.inputColorsArgb.first()` makes old multi-color entries display only their first color, satisfying
that AC without any data change.

Removed every white-sheet/calibration string (`rate_clothing_white_sheet_reminder`,
`_tap_white_sheet`, `_calibration_failed*`, `_confirm_calibration`, `_choose_different_photo`,
`_select_color`, `match_object_select_colors_title`, `match_object_continue`,
`settings_about_calibration` and its `AboutSection` line) and replaced the pick-photo reminder (shared
by both flows, as it already was) with a generic `pick_photo_reminder` that tells the user to center
the subject instead of mentioning a white sheet.

Test: rewrote `MatchObjectViewModelTest` around the new `extractAndSaveResult` (same off-main-thread
assertion shape as #20 - spinner shows before the background dispatcher is pumped, extraction runs on
the background dispatcher, then state lands on `ShowingResult`) with small local `Fake*` doubles for
`HistoryRepository`/`ThumbnailStore`/`SettingsRepository` (matching `HistoryViewModelTest`'s existing
fake-class style) rather than the old error-throwing `Proxy` stub, since this flow now actually reaches
the save step within the same coroutine. `ColorExtractorTest` rewritten for `extractMainColor`
(largest-share-in-box, out-of-box pixels ignored, explicit `region` override, single-color image).
Rate Clothing still has no dedicated ViewModel test (unchanged from #20). `./gradlew :colorspace:test
:app:testDebugUnitTest assembleDebug` all pass. Center-box overlay UI and in-app camera viewfinder are
out of scope here (issue #28).

## 2026-09-24 - Issue #20: Fix ANR - run color extraction off the main thread

`confirmCalibration` (Match Object and Rate Clothing) and `onTap` (white-balance sampling) now run
their pixel work inside `withContext(backgroundDispatcher)` (`Dispatchers.Default` by default,
injectable via the ViewModel constructor). `confirmCalibration` first sets a new `ExtractingColors`
UI state, which both screens render with the existing `LoadingStep()` spinner, then swaps to
SelectingColors/SelectingColor/result when extraction finishes. `onTap` updates state via
`_uiState.update` only if the same bitmap is still being calibrated. `ColorExtractor` now fills a
primitive `IntArray` (sized to the region, trimmed with `copyOf` if the exclusion circle skipped
pixels) instead of a boxed `ArrayList<Int>`. StrictMode thread policy (disk/network, log penalty) is
enabled in `HueAndYouApplication` only when `FLAG_DEBUGGABLE` is set.

Test: `MatchObjectViewModelTest` injects a `StandardTestDispatcher` as the background dispatcher
and checks that after `confirmCalibration()` the state is `ExtractingColors` and no pixel work has
run until the background dispatcher is pumped. `Bitmap` can't be built in plain JVM tests, so it's
allocated via Unsafe and the ViewModel gets a `pixelSourceOf` seam. Rate Clothing shares the same
pattern but has no dedicated test. PaletteImportViewModel has the same synchronous pattern and was
not touched (out of scope for #20). The on-device "no ANR with full-size photo" check is still manual.

## 2026-09-23 - Issue #17: Settings - About section with disclaimers and closeness bands

Added an "About these results" section to the bottom of `SettingsScreen` (`AboutSection`
composable, after the Backup section) - static explanatory text plus a dynamically-built list of
the four closeness bands. All disclaimer/explanation copy (how measurement works, the "best guess"
caveat, seasonal color analysis having little scientific backing, and the white-sheet calibration's
optical-brightener/lighting limits) lives in `strings.xml` as plain `settings_about_*` strings -
nothing there needed to be dynamic.

The band list is the one part the issue required to "never drift": each row is built from
`ColorMatchBand.MATCH_MAX_DELTA_E`/`CLOSE_MAX_DELTA_E`/`RELATED_MAX_DELTA_E` (the same constants
`PaletteScorer`/`ColorMatchBand.forDeltaE` already score against) rather than restating 3/8/15 as
literals in `strings.xml`, so a future change to those thresholds automatically updates this text.
Band labels reuse the existing `colorMatchBandLabel()` (from `PaletteResultView.kt`, already
`internal` and thus visible across `ui.*` packages in the app module) instead of adding parallel
`settings_about_band_*_label` strings - same label the Rate Clothing result screen already shows
for Match/Close/Related/Far. A small `formatDeltaE` helper trims the trailing `.0` off these
currently-whole-number `Double` constants (`3.0` -> "3") without hardcoding the assumption that
they'll always be whole numbers into the string resources themselves.

No new tests: this is static UI copy plus a read of existing, already-tested constants - no new
logic to unit-test, and this app has no Compose UI test harness (same Robolectric/JDK-26 gap noted
since #3). `./gradlew assembleDebug test` passes with no new failures.
