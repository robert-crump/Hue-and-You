<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Launch theme: Compose draws all screen content, so this only needs to cover
         the window background shown before the first frame. -->
    <style name="Theme.HueAndYou" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
